@echo off
REM Gradle wrapper script for Windows
REM This is a placeholder - in a real project you'd run 'gradle wrapper' to generate this

echo Gradle wrapper not generated. Run 'gradle wrapper' in the project root to generate it.
echo Or use: gradlew.bat build (after generating wrapper)
exit /b 1